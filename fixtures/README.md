# Fixture policy

All test inputs must be declared in `manifest.json` with an exact SHA-256, source, upstream/schema version, expected result, license status, and redistribution decision. Paths are relative and may not escape this directory.

Fixtures are synthetic unless explicitly documented otherwise. A real Anvil world or Mojang asset must not be committed until its redistribution rights are recorded and reviewed.

